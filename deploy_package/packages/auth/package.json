{
  "name": "@jaago/auth",
  "version": "0.0.1",
  "private": true,
  "description": "Session, JWT verify, tenant context, permission helpers for JAAGO ERP",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts",
    "test:unit": "vitest run --reporter=verbose",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@jaago/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@jaago/config-typescript": "workspace:*",
    "@types/node": "^22.15.32",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
